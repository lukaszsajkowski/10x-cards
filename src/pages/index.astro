---
/**
 * Strona główna - przekierowuje na podstawie stanu sesji:
 * - Zalogowani użytkownicy → /generate
 * - Niezalogowani → /auth/login
 *
 * UWAGA: Middleware również obsługuje to przekierowanie,
 * ta logika jest dodatkowym zabezpieczeniem.
 */

export const prerender = false;

const user = Astro.locals.user;

if (user) {
  return Astro.redirect("/generate");
}

return Astro.redirect("/auth/login");
---
